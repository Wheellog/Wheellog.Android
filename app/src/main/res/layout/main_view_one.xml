<LinearLayout android:id="@+id/page_one"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <com.cooper.wheellog.WheelView
        android:id="@+id/wheelView"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:padding="10dp"
        app:outer_thickness="40dp"
        app:inner_outer_padding="5dp"
        app:inner_thickness="25dp"
        app:inner_text_padding="0dp"
        app:box_top_padding="20dp"
        app:box_outer_padding="20dp"
        app:box_inner_padding="10dp"/>

</LinearLayout>